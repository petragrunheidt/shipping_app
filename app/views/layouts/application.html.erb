<!DOCTYPE html>
<html>
  <head>
    <title>ShippingApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application" %>
  </head>
  <nav>
  <h1>Transporte & Frete</h1>


<div class="nav links">
<%=link_to 'Modos de Transporte', transport_modes_path%> |
<%=link_to 'Veículos', vehicles_path%> |
<%= link_to 'Ordens de Serviço', orders_path%> |
<p>
<%= form_with url: search_orders_path, method: :get do |form|%>
    <%= form.label :order_query, 'Consultar Ordem de Serviço' %>
    <%= form.text_field :order_query %>
    <%= form.submit 'Buscar'%>
<% end %>
</p>
</div>


 <% if user_signed_in? %>
  <p>
  Olá, <%= current_user.description %>
  <% if current_user.admin == true %>
  <%= link_to '| Pagina de Admin', admin_dashboard_index_path %>
  <% end %>
  </p>
  <%= button_to 'Encerrar Sessão', destroy_user_session_path, method: 'delete' %>
  <% else %>
  <%= link_to 'Iniciar Sessão', new_user_session_path %>
  <% end %>
  </nav>
  <div><%= notice %></div>
  <body>
    <%= yield %>
  </body>
</html>
